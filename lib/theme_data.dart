import 'package:flutter/material.dart';



//----------- HOME PAGE ------------------

  // ------ App Bar
    const appBarBgColorLight = Color(0xffB3E5FC);
    const appBarTextColorLight = Color(0xff07273f);
    const appBarIconColorLight = Color(0xff2284d9);
    const appBarActionsColorLight = Color(0xff0c2438);

  // ------ Body
    const homePageBgColorLight = Color(0xffE1F5FE);

    //--- Start Button
      // outside color
        const homePageStartVpnOutsideColorLight = Color(0xff3ea0ef);
        const homePageStopVpnOutsideColorLight = Color(0xff044c81);

      // inside color
        const homePageStartVpnInsideColorLight = Color(0xffdff3fc);
        const homePageStopVpnInsideColorLight = Color(0xff72bcfa);

      // power icon
        const homePageStartVpnPowerColorLight = Color(0xff9a9a9a);
        const homePageStopVpnPowerColorLight = Color(0xffffffff);

      // button text
        const homePageStartVpnTextColorLight = Color(0xff999999);
        const homePageStopVpnTextColorLight = Color(0xffffffff);

    //--- Status Zone
      const homePageStartVpnStatusZoneBgColor = Color(0xffa5c2c2);
      const homePageStopVpnStatusZoneBgColor = Color(0xff52bebe);

      const homePageStartVpnDurationBgColor = Color(0xffa5c2c2);
      const homePageStopVpnDurationBgColor = Color(0xff52bebe);

